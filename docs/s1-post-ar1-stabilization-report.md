# S1 Post-AR-1 Stabilization Report

## Status

Status: S1 validated with reserves.

Recommendation for P5: GO with reserves, only if P5 keeps P0 fully isolated.

Branch: `fix/s1-post-ar1-stabilization-20260722`

## Scope

This stabilization pass is limited to security, documentation, maintainability, observability and technical consistency.

No functional change is allowed on:

- wallets
- commissions
- orders
- payments
- withdrawals
- Revenue Engine
- Commission Pool
- financial ledger
- financial snapshots
- margin and commission calculations

## Initial measurements

| Area | Result |
| --- | ---: |
| Migrations | 144 |
| Edge Functions | 27 |
| Tests in `src/__tests__` | 68 |
| Docs files | 43 |
| `SECURITY DEFINER` occurrences | 291 |
| anon grants/policies occurrences | 69 |
| public grant/revoke occurrences | 46 |
| ESLint errors before S1 | 263 |
| ESLint warnings before S1 | 26 |
| Recent post-P0 direct references to core financial tables | 0 |

## Headers review

Production HTTP headers could not be verified from this local environment because outbound HTTP failed with:

```text
Unable to connect to the remote server
```

The code defines security headers in:

- `vite.config.ts`
- `public/_headers`

Risk: Lovable can publish `_headers` as a static file instead of applying it as HTTP response headers. This must be verified in production with `curl` or Cloudflare.

| Header | Defined in code | Verified in production | Compliant | Action |
| --- | --- | --- | --- | --- |
| Strict-Transport-Security | No | Not verified | Unknown | Add at Cloudflare or hosting edge |
| Content-Security-Policy | Yes | Not verified | Unknown | Verify before strict enforcement |
| X-Content-Type-Options | Yes | Not verified | Unknown | Enforce at edge |
| X-Frame-Options | Yes | Not verified | Unknown | Enforce at edge |
| Referrer-Policy | Yes | Not verified | Unknown | Enforce at edge |
| Permissions-Policy | Yes | Not verified | Unknown | Enforce at edge |
| Cross-Origin-Opener-Policy | Yes | Not verified | Unknown | Enforce at edge |
| Cross-Origin-Resource-Policy | Yes | Not verified | Unknown | Enforce at edge |
| Cache-Control sensitive routes | Partial/route-dependent | Not verified | Unknown | Do not cache auth/admin/API responses |

### Production verification commands

Run from a machine with internet access:

```powershell
curl.exe -I https://www.lapetitebouteille.com/
curl.exe -I https://www.lapetitebouteille.com/admin
curl.exe -I https://www.lapetitebouteille.com/catalogue
curl.exe -I https://www.lapetitebouteille.com/_headers
```

Expected: `_headers` may exist as a file, but the real page responses must contain the security headers.

## Cloudflare response header rules

If Lovable does not apply headers, configure Cloudflare Response Header Modification on `www.lapetitebouteille.com/*`.

Recommended progressive values:

| Header | Value |
| --- | --- |
| Strict-Transport-Security | `max-age=15552000; includeSubDomains; preload` |
| X-Content-Type-Options | `nosniff` |
| X-Frame-Options | `DENY` |
| Referrer-Policy | `strict-origin-when-cross-origin` |
| Permissions-Policy | `camera=(self), microphone=(), geolocation=(), payment=(), usb=(), interest-cohort=()` |
| Cross-Origin-Opener-Policy | `same-origin-allow-popups` |
| Cross-Origin-Resource-Policy | `cross-origin` |
| Content-Security-Policy | see below |

Progressive CSP:

```text
default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; img-src 'self' data: blob: https:; font-src 'self' https://fonts.gstatic.com data:; connect-src 'self' https://*.supabase.co wss://*.supabase.co https://api.lovable.dev; media-src 'self' https: blob:; frame-src 'self'; object-src 'none'; base-uri 'self'; frame-ancestors 'none'; form-action 'self'; upgrade-insecure-requests
```

Reserve: `unsafe-inline` and `unsafe-eval` are tolerated only as a progressive compatibility step. A stricter CSP should be tested later with report-only mode.

## Edge Functions summary

The detailed matrix is maintained in:

- `docs/edge-functions-security-matrix.md`

Key findings:

- 27 Edge Functions are present.
- Most functions include auth, CORS and error handling patterns.
- No function currently declares explicit timeout handling in code.
- `mcp` is auto-generated by Lovable and should not be edited manually.
- Several service-role functions are internal, scheduled, webhook or notification functions; each must keep a documented caller contract.

## Supplier cost SQL controls

The production supplier price validation must be run in SQL Editor because this local environment cannot read production data directly.

Ready-to-run read-only queries are maintained in:

- `docs/supplier-cost-control-queries.md`

These queries do not mutate prices, products, orders or financial snapshots.

## ESLint stabilization

Initial ESLint status:

- Errors: 263
- Warnings: 26
- Main source: `@typescript-eslint/no-explicit-any` with 254 occurrences

Safe correction applied:

- `TextareaProps` changed from an empty interface to a type alias.

Final ESLint status after the safe correction:

- Errors: 262
- Warnings: 26
- Files with findings: 96

Rationale:

- No behavior change.
- Removes one mechanical TypeScript lint issue.
- Avoids mass refactoring of `any`, which would be too risky during stabilization.

Recommended non-regression threshold:

- Critical lint rules must not increase.
- Total ESLint errors must not exceed the current baseline until a dedicated cleanup sprint.
- P0/P1 financial or checkout files require stricter review before merging new `any`.

## Build and tests

| Test | Local | GitHub CI | Lovable | Final status |
| --- | --- | --- | --- | --- |
| `tsc --noEmit --pretty false` | Passed | To verify | To verify | Locally OK |
| `eslint .` | Failed baseline: 263 errors, 26 warnings | To verify | To verify | Reserve |
| `vitest run` | Blocked by esbuild Windows access error | To verify | To verify | Local environment blocked |
| `vite build` | Blocked by esbuild Windows access error | To verify | To verify | Local environment blocked |

Local build/test blocker:

```text
Cannot read directory "../../../../../..": Access is denied.
Could not resolve vite.config.ts / vitest.config.ts
```

This is classified as a local Windows/esbuild sandbox limitation, not as an application regression, until CI proves otherwise.

## P4.5 rollout policy

The official policy is maintained in:

- `docs/p45-extension-marketplace-rollout-policy.md`

Current P4.5 public marketplace status: blocked.

Allowed current level: internal extensions only, unless explicitly approved.

## P0 non-regression confirmation

Static scan after S1:

- Recent post-P0 financial references: 0
- No new migration created.
- No SQL change on P0.
- No Edge Function behavior change on P0.
- No functional change to wallets, commissions, orders, payments, withdrawals, ledger or snapshots.

## Residual risks

| Risk | Severity | Status | Action |
| --- | --- | --- | --- |
| Production headers not verified | High | Open | Run curl/Cloudflare verification |
| HSTS absent from code-level headers | Medium | Open | Apply via Cloudflare edge |
| Edge Functions have no explicit timeout standard | Medium | Open | Add standard in future hardening pass |
| ESLint debt remains high | Medium | Open | Dedicated cleanup sprint |
| Vitest/build blocked locally | Medium | Open | Use GitHub/Lovable CI as source of truth |
| P4.5 public opening without signatures/digests | High | Controlled | Keep public marketplace blocked |

## Final recommendation

S1 status: S1 validated with reserves.

P5 recommendation: GO with reserves.

Mandatory before broad P5 production exposure:

1. Verify production headers with `curl`.
2. Confirm GitHub/Lovable build is green.
3. Keep P4.5 public marketplace blocked.
4. Keep P0 frozen.
5. Do not merge any new P0 financial mutation inside P1/P2/P3/P4/P5 work.
